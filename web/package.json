{
  "name": "subtrans-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Vanilla JS front-end for Subtitles AI. Node test runner is used for the notifications scheduling unit tests; the browser loads these as native ES modules.",
  "scripts": {
    "test": "node --test \"__tests__/**/*.test.mjs\""
  }
}
